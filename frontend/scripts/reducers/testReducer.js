export const testReducer = (state = [], action) => {
    Object.freeze(state);
    // switch (action.type) {
    //   case RECEIVE_PRODUCTS:
    //     return action.products;
    //   default:
        return state;
    // }
  };
